const colors = {
  black: "#2f2f2f",
  white: "#fdfdfd",
  primary: "#ff9a23",
  green: "green",
  red: "red",
}
const fonts = {
  P0: "16px",
  P1: "18px",
  P2: "20px",
  P3: "24px",
  P4: "26px",
}
const gridUnit = 8
const borderRadius = {
  subtle: 1,
  round: "5px",
}
const shadows = {}
const weights = {
  light: 300,
  regular: 400,
  medium: 500,
  semiBold: 600,
  bold: 700,
  heavy: 800,
}

export const theme = {
  colors,
  fonts,
  gridUnit,
  borderRadius,
  shadows,
  weights,
}
